# 关于 sCPU 的信息

在 F4 阶段，我们以状态机的视角认识到：程序的执行，本质就是状态的变化。

只要能将 sISA 用数字逻辑映射到硬件上，我们就能实现一个简单的处理器。为了更好地指导 CPU 设计，讲义详细约束了指令格式：

```text
 7  6 5  4 3   2 1   0
+----+----+-----+-----+
| 00 | rd | rs1 | rs2 | R[rd]=R[rs1]+R[rs2]       add 指令，寄存器相加
+----+----+-----+-----+
| 10 | rd |    imm    | R[rd]=imm                 li 指令，装入立即数，高位补 0
+----+----+-----+-----+
| 11 |   addr   | rs2 | if (R[0]!=R[rs2]) PC=addr bner0 指令，若不等于 R[0] 则跳转
+----+----------+-----+
```

约束条件如下：

1. PC 位宽为 4 位，初值为 0。  
2. GPR 共有四个，每个位宽为 8 位。  
3. 支持上述三条指令。

用数字电路实现这套 sISA 指令集的 CPU，称为 **sCPU**。为简单起见，讲义首先实现了一个仅支持 `li` 指令的简化版本。

---

# 实现 li 指令的 sCPU

实现一个具体的 CPU，步骤大体相似，主要分为四步：**取指（Fetch）、译码（Decode）、执行（Execute）和更新 PC**。

## 取指（Fetch）

### 取指的概念

取指，就是根据当前 PC，从存储器中读取一条指令。当前存储指令的元件为 ROM（只读存储器），由多路选择器搭建而成。例如，下面是一个 2 行 3 列的 ROM：

![ROM2X3](https://gitee.com/alps-chenyu/typora/raw/master/image/3e4761728feb60e13a5683df85b3960461e008fb.png)

比起记住这个结构，理解它的工作原理更加重要：

> ROM 的每个存储单元都由与门构成。对每个与门而言，其输出为：
>
> $$Y = input_{0/1} \cdot output_{decoder\ 1-2}$$
>
> 当 addr 选中某一行时，该行对应的译码器输出（$output_{decoder}$）为 1，此时与门的输出就等于输入 $input_{0/1}$ 的值；而未被选中的行，由于与门的特性，输出恒为 0。
>
> 再在各列之后加一个或门，最终输出 $Q_n$ 就对应译码器所选中的那一行、第 n 列与门的输出值。

### 扩展：ROM 与 RAM

在实现 sCPU 的过程中，我们会同时用到 ROM 和 RAM。两者都能存储信息，区别在于：RAM 可写入，而 ROM 只能读取。

根据讲义，我们首先搭建 ROM 电路：

![只读存储器](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260804212514585.png)

### ROM 芯片矩阵的约定

ROM 芯片的输入主要包括两部分：编码器寻址和存储矩阵。编码器输入写作 $A_3A_2A_1A_0$，若是多输入则为 $B_3B_2B_1B_0$；存储矩阵记为 $b_{(x,y)}$，其中 $x$ 表示行（word），$y$ 表示列（depth），均按从小到大的顺序排列。sCPU 虽然简单，但输出相对复杂，明确输入输出的约定有助于减少错误。

### decoder1_2

译码器负责寻址，它的作用是根据输入选择对应的一行字（word）。该电路在结构上与 3 位 MUX2_1 没有本质区别，核心在于 decoder1_2 同一时刻只能输出一组独热码。输出端 Y0、Y1 连接的与门如同开关：译码器输出为 0 时开关关闭，为 1 时开关打开，最终结果只能是 b0 或 b1 中的一行，绝无第三种可能。

### 实现 sCPU 需要多大的 ROM？

#### 记录一个思维误区

设计 ROM 时，sCPU 所需的容量并不取决于程序执行的总步数——这是我最开始的误解。例如，在设计求和电路时，我原以为执行 35 步就需要 64 行的 ROM，但 `bner` 跳转指令能大幅缩减指令条数，实际只需 8 条指令即可完成求和。

打开 F4 笔记可知，求和运算只需 8 行指令。我们先实现一个 8 行 8 位的 ROM（Logisim 可导出图片，方便记录）：

![ROM8_8](https://gitee.com/alps-chenyu/typora/raw/master/image/ROM8_8.png)

但 PC 位宽为 4，最多可寻址 16 条指令，因此我暂时不确定两者是否相关，于是先设计了一个 16 行 8 位的 ROM：

![ROM16_8](https://gitee.com/alps-chenyu/typora/raw/master/image/ROM16_8.png)

### ROM 电路的封装

直接使用芯片非常麻烦，因此要用分线器进行封装，简化调用：

<img src="https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260804214117172.png" alt="ROM封装演示" style="zoom:50%;" />

---

## 译码（Decode）

### 译码的概念

译码，是识别当前指令的具体类型和操作数。

讲义未给出具体电路，我在此自行实现，以节省后续时间。

sCPU 的指令分三类：`li`、`add` 和 `bner`。根据指令格式，译码首先通过 8 位指令的高两位（opcode）决定操作类型：

| 编码 | 操作码        |
| ---- | :------------ |
| 10   | li 立即数操作 |
| 11   | bner 跳转操作 |
| 00   | add 加法操作  |

然后根据不同编码完成相应操作。不过，当前阶段我们只需实现 `li` 功能，因此译码可以简化：直接从 ROM 取出指令并执行即可。

---

## 执行（Execute）

### 执行的概念

执行，是对操作数进行处理，必要时更新目的寄存器。由于硬件资源的规定，我们需要设计 RAM。

### 关于 RAM

RAM 的结构与 ROM 类似，但存储信息由触发器实现，而非组合逻辑。讲义给出了 2×3 的 RAM 矩阵元件，但下图所示的电路仅支持读取，不支持写入：

![image](https://ysyx.oscc.cc/slides/2306/resources/img/tikz-images/dd11d5756391fdd050b2d709d8c44398c90b30e3.png)

### 使用 Logisim 搭建只读 RAM2_3

![RAM2_3](https://gitee.com/alps-chenyu/typora/raw/master/image/RAM2_3.png)

### 判断 sCPU 所需 RAM 大小

sCPU 有四个 8 位 GPR，所以需要 RAM4_8。按同样思路搭建如下：

![RAM4_8](https://gitee.com/alps-chenyu/typora/raw/master/image/RAM4_8.png)

### 三个思维错误记录

1. 我曾用 Enable 代替选择与门，导致严重问题：Enable 失效时，寄存器要么清零、要么保持状态，都会产生 Bug，浪费了大量时间。  
2. RAM 不仅要能读，还要能写。我最初只设计了读取，忽视了写入，导致逻辑混乱。  
3. RAM 读写的译码器其实是共享硬件的，我错误地设计了双译码器。

### 设计可读可写的 RAM2_3

这些教训，源于我没有认真阅读讲义就匆忙搭建电路，导致效率低下。讲义提供了完整功能的 RAM2_3：

![能读能写_RAM2_3](https://gitee.com/alps-chenyu/typora/raw/master/image/d107e8cb3df932af02cbf3bd4fc0758129ad24d9.png)

### 设计可读可写的 RAM4_8

![ROM4_8](https://gitee.com/alps-chenyu/typora/raw/master/image/ROM4_8.png)

---

## 更新 PC

### 更新 PC 的概念

更新 PC，是指让 PC 指向下一条指令。

根据要求，PC 位宽为 4，因此用 4 位存储器保存 PC 值。每次自动更新实际上就是一个加法器，整体可视为计数器：

![PC](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260805123657093.png)

---

## 综合四步，实现 sCPU

总结 sCPU 的实现流程：根据 PC 从 ROM 取出指令，然后从指令的 $Q_7Q_6Q_5Q_4Q_3Q_2Q_1Q_0$ 中取出 $Q_3Q_2Q_1Q_0$，存入由 $Q_5Q_4$ 编号的寄存器中。

### 设计中的 Bug

按照下图搭建的仅支持 `li` 的 sCPU，在第二步时 RAM 发生振荡，我花了很长时间才定位到这是时序问题。

![sCPU_li_1](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260805154853572.png)

![写译码器顺序匹配错误](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260805155020925.png)

解决办法很简单：ROM 直接连接 clk，而 RAM 的 clk 通过一个非门接 clk，从而消除了振荡。

---

# 完成 sCPU_li

![image-20260806161035578](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260806161035578.png)

验证结果如下：

<img src="https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260806161126894.png" alt="image-20260806161126894" style="zoom:50%;" />

完美符合讲义要求，至此 `sCPU_li` 设计完成。

---

# 实现完整的 sCPU

对于完整的 sCPU，取指阶段与之前一致，但译码阶段需要区分 `add`、`li` 和 `bner` 三种指令，因此需要使用 2-4 译码器（decoder2_4）来识别指令类型。

讲义指出：译码时需检查 opcode 字段，若为 `00` 则为 `add`，若为 `10` 则为 `li`。最适合识别输入的电路就是译码器——2-4 译码器输出的独热码可指示当前指令类型，这些独热码作为控制信号，控制各部件完成相应功能。

实现 `add` 指令面临两个关键问题：

1. **译码与使能**：如何让对应编码触发相应操作？先搭建 `add` 相关的数据通路，再考虑使能控制。  
2. **读写区分**：读取时 Enable=0，写入时 Enable=1。  
   - 加法执行步骤：  
     - 读取 rs1 的值送入加法器 A 端；  
     - 读取 rs2 的值送入加法器 B 端；  
     - 将加法结果写入 rd。

讲义提示改造 GPR 模块：

![image-20260806173025130](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260806173025130.png)

## RAM4_8 改造

改造要点：增加两个译码器，分别控制读和写，并直接输出结果。

### 第一次尝试：两写一读的 RAM4_8

![RAM4_8_4](https://gitee.com/alps-chenyu/typora/raw/master/image/RAM4_8_4.png)

该 RAM 支持一个端口读写、两个端口读取。现在需要实现：

- 当 opcode 为 `00`，执行加法；  
- 当 opcode 为 `10`，执行立即数存储；  
- 当 opcode 为 `11`，执行 `bner` 跳转。

完成这些，就实现了完整 sCPU 的读写控制。

> 说明一下我设计的 RAM 电路：$A_1A_0$ 是控制写入的端口。当 Enable = 1 时，它会随 clk 刷新电路的数据，所以 Enable 端口一定要在不需要写入时保持关闭，否则会刷出错误数据。
>
> **（这里当时的记录有误）** 我曾写成「在 `00`、`10` 时一定要关闭 Enable」，这是我当时的错误理解——实际上 `00`（add）、`10`（li）恰恰是需要写入的指令。正确的设计思路是：**只在 opcode 为 `00`、`10` 时才使能 Enable = 1，其余情况（`11`）保持为 0。**

### 译码实现

目前我已有一个输入、两个输出的 RAM，但仍需实现译码。讲义给出了大致思路：三条指令对应的 opcode 分别为 `00`、`10`、`11`，通过 $Q_7Q_6$ 译码，独热码 $Y_0, Y_2, Y_3$ 分别在高电平时对应相应指令。

从零构建过于复杂，因此我单独设计一个组合逻辑电路，对操作码和操作数进行译码：

```
 7  6 5  4 3   2 1   0
+----+----+-----+-----+
| 00 | rd | rs1 | rs2 |  → 输出 rd、rs1、rs2
+----+----+-----+-----+
| 10 | rd |    imm    |  → 输出 rd、imm
+----+----+-----+-----+
| 11 |   addr   | rs2 |  → 输出 addr、rs2
+----+----------+-----+
```

该组合逻辑根据 opcode 选择有效的输出字段，并连接到相应的数据通路。某些情况下部分输出无效，需要共用信号线进行判断。虽然讲义表述较为简略，但实际上这部分涉及不少细节，值得详细推敲。

![image-20260807171800182](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260807171800182.png)

> 我设计了下面的指令。按照指令，结果应当是 GPR0 = 3, GPR1 = 1, GPR2 = 2, GPR3 = 3。

下面是我设定的指令：

![image-20260807181001896](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260807181001896.png)

![image-20260807181742090](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260807181742090.png)

> 上面两个指令是等价的（这里纯粹是笔误，但实际结果确实是等价的）。
>
> 最后结果符合预期，判定该 CPU 设计正确。

附上电路设计：

![sCPU_li_add_1](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU_li_add_1.png)

---

# 设计能实现完整功能的 sCPU

在前面的内容中，我们已经实现了 `li` 和 `add` 的功能，现在要实现 `bner`。

## 改造 PC 计数器

要实现 `bner`，需要对 sCPU 中的 PC 计数器进行改造，使其同时具备置数和计数两种功能。为此，我设计了如下寄存器：

![image-20260809174640631](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260809174640631.png)

> 先进行理论分析，再进行功能验证。
>
> D_Flip_Flop_enable 在 F3 章节已经验证过功能的可行性，因此没什么大问题。
>
> 把功能焦点放在 D 的输入上：这里有一个类似多路选择器的电路，实际上就是电路选择器。

当 E1 = 1 时，触发置数功能；当 E1 = 0 时，则接收来自 $Counter_3, Counter_2, Counter_1, Counter_0$ 的输入。

将该电路进行例化，命名为 RegisterBy5DFlipFlop——好吧，名字起得有点蠢，我也不知道该怎么取名了。

寄存器的功能实现了，现在需要再加上一个全加器，以完成数据的全部输入。那么应该如何改造呢？我们创建一个叫做 PC_Register_2 的电路，具体如下：

![PC_Register](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260809175902637.png)

经过验证，这个电路实现的功能没有问题。现在就需要考虑如何实现一个完整的 sCPU 了。在此之前，我先提供改造指令输入的思路：

![PixPin_2026-08-09_18-02-20](https://gitee.com/alps-chenyu/typora/raw/master/image/PixPin_2026-08-09_18-02-20.png)

> 沿着这个思路，具体还要怎么改造，就必须回到 `bner` 这个指令本身：`bner` 指令的作用，实际上是将 RS2 和 RS0 进行比较，如果不相等就跳转到 Addr，而 Addr 存储在 ROM 当中。所以这个电路大概也就拼凑出来了。

## 首次拼接与循环失败

先给一个大致的思路（下方电路实现功能不齐全，仅作为一种思路）：

![sCPU_Part_I](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU_Part_I.png)

这里实际上有一个逻辑上的误区。

![关于这只电路的错误](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260810192615828.png)

我修改了这个电路，最后长成了如下图所示的电路：

![sCPU](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU.png)

我们来回顾 F4 中求和指令的编码：

> 0: li r0, 10   # 这里是十进制的 10
> 1: li r1, 0
> 2: li r2, 0
> 3: li r3, 1
> 4: add r1, r1, r3
> 5: add r2, r2, r1
> 6: bner0 r1, 4
> 7: bner0 r3, 7

编码所表征的代码如下：

![image-20260810194609920](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260810194609920.png)

> 但是，我的设计明显有问题——它没有实现正常的循环功能。我必须重新思考 sCPU 的 `bner0` 指令是否在设计上出了错误。
>
> 它实际上并没有完成数据的跳转。于是我先后从两个角度排查：先思考跳转的数据传输是怎么完成的；再考虑是不是时序的设置刚好让它错过了数据的跳变。我首先尝试修改电路，改成如下所示的电路，但惊讶地发现它并没有从第 6 条转到第 4 条，而是一直卡在第 7 条。

![sCPU_2](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU_2.png)

> 最开始，我觉得问题出在置数上——这简单，我再搭建一个电路去验证 $D_3D_2D_1D_0$ 是什么数字就好了。
>
> 后来我才发现，很可能是译码的过程理解有问题。这个问题的根源在于：不想搞清楚细节，就去强制地设计电路。这是不对的，看似节约了时间，实际上增加了电路的混乱程度。

## 8月11日：重设指令并定位振荡

通过 8 月 11 日的研究，我设置了如下的指令序列：

|   **$b_0$**   | **0000_0000** |
| :-----------: | :-----------: |
|   **$b_1$**   | **1000_1010** |
|   **$b_2$**   | **1001_0000** |
|   **$b_3$**   | **1010_0000** |
|   **$b_4$**   | **1011_0001** |
|   **$b_5$**   | **0001_0111** |
|   **$b_6$**   | **0010_1001** |
|   **$b_7$**   | **1101_0101** |
|   **$b_8$**   | **1101_1111** |
|   **$b_9$**   | **0000_0000** |
|  **$b_{10}$** | **0000_0000** |
|  **$b_{11}$** | **0000_0000** |
|  **$b_{12}$** | **0000_0000** |
|  **$b_{13}$** | **0000_0000** |
|  **$b_{14}$** | **0000_0000** |
|  **$b_{15}$** | **0000_0000** |

本来最开始的指令是这样的：

![指令](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260811152714014.png)

但是我增加了一个初始状态：0000_0000。

> 我一度认为，电路的问题主要是为了解决来自 clk 的异步时钟——可能把数字送进去时就已经迟了。（后来排除问题时才发现，这样做其实是合理的，并没有什么问题——真正的原因是没有 clk 的非门，时序电路会产生振荡，从而让电路出错。）

![image-20260811154224917](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260811154224917.png)

错误的根源之一在这里：当 $Q_3Q_2Q_1Q_0$ = 0111 时，本应跳转到 0101，但它没有跳转——因为此时比较器输出的相等信号恰好相等。我突然明白自己错在哪了：**Enable 被关闭了，同时也就读不到数据了。** 于是我又回到之前讲义给的思路，修改成如下所示的电路，果然起效了。

![sCPU_3](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU_3.png)

## 排查 add 指令未写入

但又遇到新的问题：我给 RAM 装上输出端，用来显示 RAM 存储器中的 $Q_3Q_2Q_1Q_0$。

最后猛然发现，除了第零行的数据以外，$Q_2Q_1Q_0$ 的数据根本就没有更新。这是一个巨大的问题——意味着要么是 RAM 电路出了问题，要么是译码方式出了问题，甚至可能还有其他新问题。我之前进行过验证，RAM 应该是没有问题的。

深入分析后发现：数据的写入其实只存在于 `add` 和 `li` 指令，`bner` 指令根本没有涉及到数据的写入。问题就在这里——当 `add` 和 `li` 指令激活时，数据一定没有被写入 RAM，所以才出现了 Q2、Q1、Q0 数据全为 0 的结果，这就是求和过程产生错误的原因。

所以要解决这个问题，首先要查看数据为什么没有写入。经过仔细排查，发现 `li` 指令其实是能正常写入数据的，但 `add` 指令并不能正常完成任务。所以现在的任务就是排查 `add` 指令是否出现了错误。这是当前 sCPU 的示意图，当然 `add` 是错误的——目的就是要排查 `add` 指令哪里有错误。

![sCPU_4](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU_4.png)

换句话说，还是回归到了主题：很大程度上是译码电路有问题，正确修正译码电路，最后的结果可能就是正确的。

> 找到问题了——问题出在这里：这部分的逻辑原本没有覆盖到 Y3，因此需要修改。

![部分译码电路出现的问题](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260811183455451.png)

这里的电路，应该是在逐渐逼近正确的方法了。

# 8月13日

![RAM电路测试](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260813125245661.png)

不过，其实 RAM 电路本身并没有实质问题——之前一直怀疑是 RAM 的故障，最终查明只是单纯的输出接线接错了。

# 重新构建 sCPU3

> 重新构建的 sCPU2，因为操作时间不连续，已经来来回回折腾了好久，打算直接重做一份。

再回忆一下指令：

```
 7  6 5  4 3   2 1   0
+----+----+-----+-----+
| 00 | rd | rs1 | rs2 | R[rd]=R[rs1]+R[rs2]       add 指令，寄存器相加
+----+----+-----+-----+
| 10 | rd |    imm    | R[rd]=imm                 li 指令，装入立即数，高位补 0
+----+----+-----+-----+
| 11 |   addr   | rs2 | if (R[0]!=R[rs2]) PC=addr bner0 指令，若不等于 R[0] 则跳转
+----+----------+-----+
```

调试了 n 次，终于是做出来了：

![image-20260813160139697](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260813160139697.png)

![sCPU](https://gitee.com/alps-chenyu/typora/raw/master/image/sCPU-1786608070395-1.png)
