# 计算机状态机模型

> 本讲义重在传递思想，因此我的学习方式也侧重于吸收其中的核心理念。与 F3 笔记不同，这里不再逐步骤手动实现，而是试图概括作者想要传达的思考脉络。通读全文后，我认为其主旨在于：从 ISA、代码和数字电路层面，阐述如何实现计算机的状态机模型。要真正以状态机的视角去思考，首先需要建立起这些基本概念。
>
> 本人 阿尔卑斯完成结合AI辅助，写下这段笔记， 希望大家喜欢。

---

# 处理器的组成与工作原理

> 计算机本质上是一台机械地执行数据处理的装置。要理解“机械”二字的含义，首先需要弄清什么是指令。

---

# 指令与编码

作为初学者，我这样理解：计算机的指令，就是它要执行的操作步骤，而每个操作步骤都包含两个要素：**做什么**（操作）和**对谁做**（操作对象）。这有点像数学函数 \( f(t) \)——\( f \) 代表操作，\( t \) 代表操作数。

用讲义的术语来表述，就是**操作码**（opcode）和**操作数**（operand）：

```text
+--------+---------------+
| opcode |    operand    |
+--------+---------------+
```

---

# 关于寄存器

若要让计算机连续执行一系列操作，就必须有记忆元件来暂存每一步的中间结果。这个记忆元件就是**寄存器**（Register Set / Register File）。寄存器为指令提供了快速访问的“工作区”，使得计算可以按步骤串联起来。

---

# 一个简单的处理器模型

我们给出一个简易的指令格式示例：

```text
 7  6 5  4 3   2 1   0
+----+----+-----+-----+
| 00 | rd | rs1 | rs2 |   add 指令：R[rd] = R[rs1] + R[rs2]
+----+----+-----+-----+
| 10 | rd |    imm    |   li  指令：R[rd] = imm（高位补 0）
+----+----+-----+-----+
```

对应机器码示例：

```text
00100001   → add 指令，将 R[0] 与 R[1] 相加，结果存入 R[2]
10110000   → li  指令，将立即数 0000 装入 R[3]
10000101   → li  指令，将立即数 0101 装入 R[0]
```

---

# 存储程序思想

程序本质上就是一段按顺序排列的指令序列。电子计算机之所以智能，是因为它能够让程序自动控制自身的执行过程。计算机每执行完一条指令，就应该接着取下一条指令。为此，需要一个专用部件来指示当前执行到哪一条指令，这个部件就是**程序计数器（PC）**。

有了 PC 后，计算机只需重复以下简单循环：

```text
重复执行：
  1. 从 PC 所指向的存储器位置取出指令
  2. 执行该指令
  3. 更新 PC（通常指向下一条指令）
```

这样，计算机从 PC = 0 开始，依次执行第 0 条、第 1 条、第 2 条……指令，周而复始。这就是“**存储程序**”（Stored-Program）的核心思想——将程序和数据一同存放在存储器中，由控制器自动逐条取指和执行。

---

# 条件跳转指令增强灵活性

为了使计算机能够根据条件改变执行流程，而不是机械地顺序执行，我们引入条件跳转指令，例如 `bner0`：

```text
 7  6 5         2 1  0
+----+---- -----+-----+
| 11 |   addr   | rs2 |   bner0 指令：若 R[0] != R[rs2]，则 PC = addr
+----+----------+-----+
```

这条指令允许程序在满足条件时跳转到指定地址，从而支持分支、循环等控制结构，极大地增强了指令执行的灵活性。有了条件跳转，计算机才真正具备了“决策”能力，能够应对复杂多变的任务需求。

---

通过以上层层递进的介绍，我们逐步构建起从指令、寄存器、PC 到存储程序和条件跳转的基本认知框架。这些概念共同支撑起了计算机状态机模型，也为后续深入理解处理器设计奠定了基础。

# 第一个示例

讲义的作者封装了一组极简指令：

~~~
li   r0, imm   # 将立即数 imm 存入寄存器 r0
add  r1, r2, r3 # r1 = r2 + r3
bner0 r1, 4    # 若 R[0] != R[1]，则跳转到 PC = 4 处的指令
~~~

这些指令虽然极其简单，却已足够支撑我们完成一个经典任务：**计算 1 + 2 + ... + 10**。

讲义中虽然给出了代码设计思路，但如果我们只是照着抄一遍，收获终究有限。更有价值的做法是：**抛开现成答案，从零开始思考——仅凭手中这几把“工具”，如何自行设计出这段程序？**

---

## 从电路视角审视问题

回顾之前数字电路的学习经验，我们会发现一个核心技巧：

- 在累加 **+4** 之前，我们必须记住 **1 + 2 + 3**（即 6）这个中间结果；
- 在累加 **+5** 之前，又必须记住 **6 + 4**（即 10）这个新结果；
- 依此类推……

这意味着，整个计算过程离不开两类“记忆元件”（寄存器）：

1. **总和寄存器**：用来存放“截至目前已累加的和”；
2. **计数器寄存器**：用来记住“下一个要加几”。

有了这两份“记忆”，计算机才能在每一步都清楚地知道：**当前累加到了哪一步？下一步该加几？**

---

## 关于“顺序执行”的可行性

一个值得注意的问题是：**如果不使用 `bner0` 跳转指令，仅靠顺序执行，能完成这个求和吗？**

答案是**可以**，但代价很大——我们只能把循环“展开”：

```asm
li   r0, 1     # 总和 = 1
add  r0, r0, 2 # 总和 = 1 + 2
add  r0, r0, 3 # 总和 = 1 + 2 + 3
add  r0, r0, 4 # 总和 = 1 + 2 + 3 + 4
...（一直写到 +10）
```

这种写法虽然直观，但需要连续写出 10 条 `add` 指令。如果要算到 100 呢？指令数量会线性膨胀，显然不够优雅。而 `bner0` 的魅力正在于此——它让我们能够**用固定的指令条数表达任意长度的重复计算**，将程序的“长度”从数据规模中解放出来。这正是状态机模型中“循环”与“分支”控制流的意义所在。

讲义提供了具体的写法。

```text
0: li r0, 10   # 这里是十进制的10
1: li r1, 0
2: li r2, 0
3: li r3, 1
4: add r1, r1, r3
5: add r2, r2, r1
6: bner0 r1, 4
7: bner0 r3, 7
```




![image-20260801222614756](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260801222614756.png)



我使用AI重新整理了一下，毕竟上面是人工计算的，也有可能笔误

| 步数 | 执行后状态 (PC, r0, r1, r2, r3) | 说明                                                     |
| :--- | :------------------------------ | :------------------------------------------------------- |
| 初始 | (0, 0, 0, 0, 0)                 | 初始状态                                                 |
| 1    | (1, 10, 0, 0, 0)                | `li r0, 10`                                              |
| 2    | (2, 10, 0, 0, 0)                | `li r1, 0`                                               |
| 3    | (3, 10, 0, 0, 0)                | `li r2, 0`                                               |
| 4    | (4, 10, 0, 0, 1)                | `li r3, 1`                                               |
| 5    | (5, 10, 1, 0, 1)                | 第1次循环：`add r1, r1, r3` → r1=1                       |
| 6    | (6, 10, 1, 1, 1)                | `add r2, r2, r1` → r2=1                                  |
| 7    | (4, 10, 1, 1, 1)                | `bner0 r1, 4` → r1≠10，跳转回4                           |
| 8    | (5, 10, 2, 1, 1)                | 第2次循环：r1=2                                          |
| 9    | (6, 10, 2, 3, 1)                | r2=1+2=3                                                 |
| 10   | (4, 10, 2, 3, 1)                | 跳转回4                                                  |
| 11   | (5, 10, 3, 3, 1)                | 第3次循环：r1=3                                          |
| 12   | (6, 10, 3, 6, 1)                | r2=3+3=6                                                 |
| 13   | (4, 10, 3, 6, 1)                | 跳转回4                                                  |
| 14   | (5, 10, 4, 6, 1)                | 第4次循环：r1=4                                          |
| 15   | (6, 10, 4, 10, 1)               | r2=6+4=10                                                |
| 16   | (4, 10, 4, 10, 1)               | 跳转回4                                                  |
| 17   | (5, 10, 5, 10, 1)               | 第5次循环：r1=5                                          |
| 18   | (6, 10, 5, 15, 1)               | r2=10+5=15                                               |
| 19   | (4, 10, 5, 15, 1)               | 跳转回4                                                  |
| 20   | (5, 10, 6, 15, 1)               | 第6次循环：r1=6                                          |
| 21   | (6, 10, 6, 21, 1)               | r2=15+6=21                                               |
| 22   | (4, 10, 6, 21, 1)               | 跳转回4                                                  |
| 23   | (5, 10, 7, 21, 1)               | 第7次循环：r1=7                                          |
| 24   | (6, 10, 7, 28, 1)               | r2=21+7=28                                               |
| 25   | (4, 10, 7, 28, 1)               | 跳转回4                                                  |
| 26   | (5, 10, 8, 28, 1)               | 第8次循环：r1=8                                          |
| 27   | (6, 10, 8, 36, 1)               | r2=28+8=36                                               |
| 28   | (4, 10, 8, 36, 1)               | 跳转回4                                                  |
| 29   | (5, 10, 9, 36, 1)               | 第9次循环：r1=9                                          |
| 30   | (6, 10, 9, 45, 1)               | r2=36+9=45                                               |
| 31   | (4, 10, 9, 45, 1)               | 跳转回4                                                  |
| 32   | (5, 10, 10, 45, 1)              | 第10次循环：r1=10                                        |
| 33   | (6, 10, 10, 55, 1)              | r2=45+10=55                                              |
| 34   | (7, 10, 10, 55, 1)              | `bner0 r1, 4` → r1==r0，不跳转，PC=7                     |
| 35   | (7, 10, 10, 55, 1)              | `bner0 r3, 7` → r3=1≠10，跳转自身，PC仍为7，此后无限循环 |

> 计算10以内的奇数之和
>
>尝试用上述指令编写一个程序, 求出10以内的奇数之和, 即计算`1+3+5+7+9`. 编写后, 尝试列出处理器状态的变化过程, 以此来检查你编写的程序是否正确.

~~~
加计数规律 ： 1 + 3 + 5 + 9; 

原来1 + 2 + 3 + ... + 10 运行指令的步骤
0: li r0, 10   # 这里是十进制的10
1: li r1, 0
2: li r2, 0
3: li r3, 1
4: add r1, r1, r3
5: add r2, r2, r1
6: bner0 r1, 4
7: bner0 r3, 7

那么如何改动这个指令呢？
我尝试了很多次，然后得出这样的结果

0: li r0, 9  # 将10改成9，原因就是要改为最后加数字
1: li r1, 1	 # 	
2: li r2, 1  # 
3: li r3, 2	 # 让个数呈现每次 +2 程度的递增
4: add r1, r1, r3
5: add r2, r2, r1
6: bner0 r1, 4
7: bner0 r3, 7

将实现效果如下表展示， 会发现正好能满足功能。
~~~



![image-20260802132458813](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802132458813.png)





# 指令集架构的状态机模型

状态机的定义包含如下部分（并非严谨数学定义，但是足够去理解）：

>1. 状态集合 S = {$S_1$, $S_2$, $S_3$, ...}
>2. 激励事件集合E
>3. 状态转移规则 ： next : S × E -> S
>   1. 描述每个状态在不同激励事件下的次态(next state), 即二元函数next(S, E)给出了在状态S下接收到事件E之后的次态
>
>4. 初始状态$S_0$ ∈ S

将这套定义融入ISA的 视角

1. -> ISA的状态集合，应当是数字电路中存储的稳定信息的元素， 应该有PC， GPR 和 内存。 S = {（PC， R， M）}

2. -> 激励事件集合， 在ISA中， 执行指令会改变状态， 因此**执行指令**就是这个状态机的激励事件

3. -> 状态转移规则， 按照定义， ISA中， 状态转移规则用于描述 ”在某个状态下执行某指令的次态“，也就是指令它做表征的语义， 它约定了执行某指令后，状态机应该发生怎样的变化， 从而从一个状态转移到另一个。

4. -> 初始状态, 在未执行任何指令之前的状态.

以上述的sISA为例， 状态机模型如下 ：
状态集合 S = {（PC, R, M）} = {(PC, r0, r1, r2, r3)}. 例如在数列求和例子中， 某个状态可以表示为： 

$S_k$ = (5, 10, 1, 0, 1) = [10001010, 10010000, 10100000, 10110001, 

​			 00010111, 00101001, 11010001, 11011111]。



在sISA中， 指令并不会修改内存中存储的信息，为了简化表示，我们可以省略状态中对内存的描述： $S_k$ = (5, 10, 1, 0, 1)。

sISA 作为一种具体的ISA， 激励事件同样是执行指令.

在ISA中， 状态机转移规则就是用下3条指令的语义：

```text
 7  6 5  4 3   2 1   0
+----+----+-----+-----+
| 00 | rd | rs1 | rs2 | R[rd]=R[rs1]+R[rs2]       add指令, 寄存器相加
+----+----+-----+-----+
| 10 | rd |    imm    | R[rd]=imm                 li指令, 装入立即数, 高位补0
+----+----+-----+-----+
| 11 |   addr   | rs2 | if (R[0]!=R[rs2]) PC=addr bner0指令, 若不等于R[0]则跳转
+----+----------+-----+
```

例如呢， $S_{k+1}$ = $next_{(s_k, 00101001)}$, 根据之前的知识点， 我们可以判断，该条指令的行为是 R[2] = R[2] + R[1],

因此$S_{k+1}$ = (6, 10, 1, 1, 1) 这其实就是上述数列求和例子执行过程中的其中一步。

初始状态 $S_0$ = (0, 0, 0, 0, 0).



>除了指令的语义之外, ISA还包括输入输出, 系统状态, 中断异常, 虚存管理, 内存模型. 随着学习进度的深入, 我们会依次向你介绍这些概念, 你也有机会将这些概念实现到自己设计的处理器中, 让处理器变得更强大.

# C语言入门

示例 ：

~~~C
#include <stdio.h>

/* 1 */ int main() {
/* 2 */   int x = 1;
/* 3 */   int y = 2;
/* 4 */   int z = x + y;
/* 5 */   printf("z = %d\n", z);
/* 6 */   return 0;
/* 7 */ }
~~~



在这个例子中, 我们已经介绍了C语言中一些关键的组成要素:

1. 变量, 它们是C程序处理的对象
2. 语句, 它们是C程序处理的操作流程
3. 输入输出函数, 它们是C程序中让变量与外界交互的桥梁

![image-20260802140449417](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802140449417.png)

稍微修改了一下代码

![image-20260802140625847](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802140625847.png)

# C程序的状态机模型

>C 语言的状态机模型， 其实和ISA 有着某种相似之处 ，我们也可以从状态机的视角去理解C语言。

还是从状态机四大定义的视角去分析 ： 

1. 状态集合 ： 1.变量 - 很明显能开出来，是存储信息的对象。 还有一个不太容易看出来的隐性变量。 程序计数器PC， 在C语言中， 一个状态是一组具体变量和PC， 而全体状态的集合S则是变量和PC所有取值的组合， 即 

S = {(PC, V)}, 这就是变量的状态。

2. 激励事件集合， 在C程序中， 执行语句状态会发生改变， 因此执行语句就是状态机的执行事件
3. 状态转移规则， C语言执行时，本身的语义， 其实就是状态转移的规则。
4. 初始状态。 未执行任何语句之前的状态， 即 $S_0$ = (main函数第一句语句的位置， $V_0$)



```text
PC  x  y  z
(2, ?, ?, ?)    # 初始状态
(3, 1, ?, ?)    # 执行PC为2的语句后, x更新为1, PC更新为下一条语句的位置
(4, 1, 2, ?)    # 执行PC为3的语句后, y更新为2, PC更新为下一条语句的位置
(5, 1, 2, 3)    # 执行PC为4的语句后, z更新为x + y, PC更新为下一条语句的位置
(6, 1, 2, 3)    # 执行PC为5的语句后, 终端输出"z = 3", PC更新为下一条语句的位置
(END, 1, 2, 3)  # 执行PC为6的语句后, 从main函数返回, 程序执行结束
```



# 用C来实现数列求和

求 1 + 2 + 3 + ... + 10 的求和

~~~C
int main(){
    int sum = 0; 
    int  i = 1; 
    do {
        sum = sum + i;
        i = i + 1;
    } while(i <= 10)
    printf("sum = %d\n", sum);
    return 0;
}
~~~

1. 变量的命名可以更直观地反映出其用途, 但汇编语言中, GPR的用途只能根据上下文推断
2. 循环的表达更清晰, 可以直接区别循环条件和循环体, 但在汇编语言中, 循环条件和循环体都是指令, 需要根据上下文推断

根据上述程序的行为, 前若干条语句的执行过程如下:

```text
PC sum i
(2, ?, ?)    # 初始状态
(3, 0, ?)    # 执行PC为2的语句后, sum更新为0, PC更新为下一条语句的位置

```

我手写了状态的转换 ：

![C语言状态机](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802150008385.png)



# 数字电路的状态机模型

这里我不再自己归纳，我觉得讲义写的已经很好了。

>
>
>- 状态集合. 在数字电路中, 只有时序逻辑电路才能存储信息, 因此一个状态是时序逻辑元件所存储的具体信息, 而全体状态的集合则是时序逻辑元件所能存储信息的所有组合.
>
>- 激励事件集合. 既然时序逻辑元件表征了数字电路的状态, 而时序逻辑元件的内部状态可以通过其输入端改变(例如可以通过输入端将数据写入D触发器), 我们可以将数字电路看成以下模型:
>
>  ```text
>      +------------------+
>  +-->| Sequential Logic |----+
>  |   +------------------+    |
>  | next state                | current state
>  |                           |
>  |  +---------------------+  |
>  +--| Combinational Logic |<-+
>     +---------------------+
>  ```
>
>  也即, 让时序逻辑元件的状态发生变化的, 其实是组合逻辑电路输出的信号, 因此组合逻辑电路就是这个状态机的激励事件.
>
>- 状态转移规则. 时序逻辑元件的状态具体应如何变化, 是由组合逻辑电路的具体逻辑决定的.
>
>- 初始状态. 即电路在复位时, 时序逻辑元件的状态.

这是我之前搭建的实现数列求和电路的模型

![image-20260802150822575](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802150822575.png)



先将这份复杂的电路连线改变一下，

我们很明显就得到一个结论， 将Counter16(图片笔误写成了16bit， 但不用在意细节)

将Counter16的每次输出都记作 $A_3 A_2 A_1 A_0$

则最后的输出 $Q_3^n Q_2^n Q_1^n Q_0^n$ = $Q_3^{n-1} Q_2^{n-1} Q_1^{n-1} Q_0^{n-1}$ + $A_3 A_2 A_1 A_0$ （溢出自动补位）

按照这种思路来分析， 这个电路就变得异常简单了。

![image-20260802154250101](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802154250101.png)

# 在计算机上执行C程序

我们都知道， 机器就是机器， 他只能执行指令， 我们给他什么指令， 他就能根据指令一步步去执行， 但是这里有一个问题，当我们去写高级语言的时候， 比如C， Python， Java， 计算机又读不懂程序， 它凭什么按照我们写的逻辑去执行程序？ 我们先来理解C语言如何是代码的功能， 实际上是编译器首先将C编译为计算机能读懂的指令， 它能按照写入的指令一步步地去执行。

这个将高级语言转化为指令的过程，我们就叫做编译，而完成这个过程的程序，我们就叫做编译器。



我们还可以去尝试了解编译器需要完成哪些工作， 我们先将两个状态机的内容列出来。



![image-20260802160100108](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802160100108.png)



对比发现， 其实编译器的工作， 就是将C语言的状态机翻译成ISA的状态机， 也就是构造两个工作

1. 从状态集合上

   ![image-20260802161226292](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802161226292.png)

编译器，要将C语言的程序计数器和变量的状态 映射到 指令的 程序计数器， 寄存器 和内存的状态。

2. 从状态的转移规则来看

   ![image-20260802161403569](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802161403569.png)

   编译将C语言的语句变成指令序列

 要使得 ：

![image-20260802161637473](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802161637473.png)

这里的意思， 其实是next和compile S 运算可交换， 为后面的推到

把这里抽象成函数就好理解 f(g(x,y)) = g($f_1$(x), $f_2(y)$), 这里的$f_1$ 和 $f_2$ 本质上就是compile 函数对于不同变量的不同表达， 当然 ，这是我自己理解方式， 作为初学者， 如果理解地不对请多包含。

>结合数列求和的例子理解编译器的工作
>
>我们已经给出了数列求和的C程序和相应的指令序列, 尝试从状态机的视角理解它们之间的联系。

之前写过C和sISA的一种运算的不同表达 ：

sISA ： 

| (0, 0, 0, 0, 0) | 初始状态           |
| :-------------- | :----------------- |
| 1               | (1, 10, 0, 0, 0)   |
| 2               | (2, 10, 0, 0, 0)   |
| 3               | (3, 10, 0, 0, 0)   |
| 4               | (4, 10, 0, 0, 1)   |
| 5               | (5, 10, 1, 0, 1)   |
| 6               | (6, 10, 1, 1, 1)   |
| 7               | (4, 10, 1, 1, 1)   |
| 8               | (5, 10, 2, 1, 1)   |
| 9               | (6, 10, 2, 3, 1)   |
| 10              | (4, 10, 2, 3, 1)   |
| 11              | (5, 10, 3, 3, 1)   |
| 12              | (6, 10, 3, 6, 1)   |
| 13              | (4, 10, 3, 6, 1)   |
| 14              | (5, 10, 4, 6, 1)   |
| 15              | (6, 10, 4, 10, 1)  |
| 16              | (4, 10, 4, 10, 1)  |
| 17              | (5, 10, 5, 10, 1)  |
| 18              | (6, 10, 5, 15, 1)  |
| 19              | (4, 10, 5, 15, 1)  |
| 20              | (5, 10, 6, 15, 1)  |
| 21              | (6, 10, 6, 21, 1)  |
| 22              | (4, 10, 6, 21, 1)  |
| 23              | (5, 10, 7, 21, 1)  |
| 24              | (6, 10, 7, 28, 1)  |
| 25              | (4, 10, 7, 28, 1)  |
| 26              | (5, 10, 8, 28, 1)  |
| 27              | (6, 10, 8, 36, 1)  |
| 28              | (4, 10, 8, 36, 1)  |
| 29              | (5, 10, 9, 36, 1)  |
| 30              | (6, 10, 9, 45, 1)  |
| 31              | (4, 10, 9, 45, 1)  |
| 32              | (5, 10, 10, 45, 1) |
| 33              | (6, 10, 10, 55, 1) |
| 34              | (7, 10, 10, 55, 1) |
| 35              | (7, 10, 10, 55, 1) |

C语言

![C语言状态机](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802150008385.png)

| 步骤 | 状态 (PC, sum, i) | 执行的 C 语句               | 下一跳 / 备注                   |
| :--- | :---------------- | :-------------------------- | :------------------------------ |
| 1    | (0, ?, ?)         | 初始状态                    | 准备执行 `sum = 0`              |
| 2    | (1, 0, ?)         | `sum = 0;`                  | PC → 2                          |
| 3    | (2, 0, 1)         | `i = 1;`                    | PC → 3，进入循环                |
| 4    | (3, 1, 1)         | `sum = sum + i;` (0+1=1)    | PC → 4                          |
| 5    | (4, 1, 2)         | `i = i + 1;` (1+1=2)        | PC → 5                          |
| 6    | (5, 1, 2)         | 判断 `2 <= 10`，成立        | PC → 3（跳转回循环头）          |
| 7    | (3, 3, 2)         | `sum = sum + i;` (1+2=3)    | PC → 4                          |
| 8    | (4, 3, 3)         | `i = i + 1;` (2+1=3)        | PC → 5                          |
| 9    | (5, 3, 3)         | 判断 `3 <= 10`，成立        | PC → 3                          |
| 10   | (3, 6, 3)         | `sum = sum + i;` (3+3=6)    | PC → 4                          |
| 11   | (4, 6, 4)         | `i = i + 1;` (3+1=4)        | PC → 5                          |
| 12   | (5, 6, 4)         | 判断 `4 <= 10`，成立        | PC → 3                          |
| 13   | (3, 10, 4)        | `sum = sum + i;` (6+4=10)   | PC → 4                          |
| 14   | (4, 10, 5)        | `i = i + 1;` (4+1=5)        | PC → 5                          |
| 15   | (5, 10, 5)        | 判断 `5 <= 10`，成立        | PC → 3                          |
| 16   | (3, 15, 5)        | `sum = sum + i;` (10+5=15)  | PC → 4                          |
| 17   | (4, 15, 6)        | `i = i + 1;` (5+1=6)        | PC → 5                          |
| 18   | (5, 15, 6)        | 判断 `6 <= 10`，成立        | PC → 3                          |
| 19   | (3, 21, 6)        | `sum = sum + i;` (15+6=21)  | PC → 4                          |
| 20   | (4, 21, 7)        | `i = i + 1;` (6+1=7)        | PC → 5                          |
| 21   | (5, 21, 7)        | 判断 `7 <= 10`，成立        | PC → 3                          |
| 22   | (3, 28, 7)        | `sum = sum + i;` (21+7=28)  | PC → 4                          |
| 23   | (4, 28, 8)        | `i = i + 1;` (7+1=8)        | PC → 5                          |
| 24   | (5, 28, 8)        | 判断 `8 <= 10`，成立        | PC → 3                          |
| 25   | (3, 36, 8)        | `sum = sum + i;` (28+8=36)  | PC → 4                          |
| 26   | (4, 36, 9)        | `i = i + 1;` (8+1=9)        | PC → 5                          |
| 27   | (5, 36, 9)        | 判断 `9 <= 10`，成立        | PC → 3                          |
| 28   | (3, 45, 9)        | `sum = sum + i;` (36+9=45)  | PC → 4                          |
| 29   | (4, 45, 10)       | `i = i + 1;` (9+1=10)       | PC → 5                          |
| 30   | (5, 45, 10)       | 判断 `10 <= 10`，成立       | PC → 3（最后一次进入循环体）    |
| 31   | (3, 55, 10)       | `sum = sum + i;` (45+10=55) | PC → 4                          |
| 32   | (4, 55, 11)       | `i = i + 1;` (10+1=11)      | PC → 5                          |
| 33   | (5, 55, 11)       | 判断 `11 <= 10`，**不成立** | PC → 6（退出循环）              |
| 34   | (6, 55, 11)       | `printf("sum = %d\n", 55);` | PC → 7                          |
| 35   | (7, 55, 11)       | `return 0;`                 | **终止 / 停机（状态不再改变）** |

将这两套状态机模型进行对比，我们不难发现，将C语言的状态机，转化为sISA的状态机模型，主要是做了两点工作。

1. 将{PC, SUM, i} 状态机集合 转化为 {PC, R0, R1, R2，R3}的状态机映射
2. 将C语言代码和它本身的语义 转化为 add， li， bner0 的指令和它背后的语义。

这样， 机器就读懂了应该做什么， 就能够去是是实现代码。

>由此可看， 当年发明C语言的人，真的是天才



![image-20260802204931800](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260802204931800.png)

这是一个网站提供的功能， 能将一段C语言，转换对应的X86汇编指令集。

通过这个图片，能大致理解，编译器的工作，最后的目标都是将C语言转换成对应的指令集，让计算机来完成相应的操作。

### CPU设计 = 根据ISA设计数字电路

同样地, 既然ISA和数字电路都可以从状态机视角去理解, 我们也可以尝试去了解CPU设计应该如何开展. 我们先将两个状态机的内容列出来(此处暂不关心初始状态):

|              |    ISA     |      数字电路      |
| :----------: | :--------: | :----------------: |
|     状态     |            |    时序逻辑电路    |
|   激励事件   |  执行指令  |    处理组合逻辑    |
| 状态转移规则 | 指令的语义 | 组合逻辑电路的逻辑 |

因此, CPU设计的工作就是用数字电路的状态机实现ISA的状态机

其中, 表示CPU电路状态机的第个状态. 用自然语言来描述, 则是: ISA执行一条和指令后的状态, 与设计出的CPU在组合逻辑电路控制下的次态, 语义上是等价的.

具体地, 对比两个状态机后可以得知, CPU设计需要完成以下工作:

- 用数字电路的状态实现ISA的状态, 也即, 用时序逻辑电路实现PC, GPR和内存
- 用数字电路的状态转移规则实现ISA的状态转移规则, 也即, 用组合逻辑电路实现指令的功能

## 程序, ISA和CPU之间的联系

到此, 我们可以来简单梳理程序, ISA和CPU之间的联系:

1. 根据ISA手册的功能描述, 画一张CPU的结构图 -> 处理器微结构设计
2. 根据结构图设计具体的电路 -> 逻辑设计
3. 开发程序 -> 软件编程
4. 将程序翻译成ISA手册中描述的指令序列 -> 编译
5. 在CPU上执行程序 = 用程序编译出的指令序列控制CPU电路进行状态转移

