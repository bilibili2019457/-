# 目标

通过线上网站学习Verilog语言

我们给大家推荐Verilog在线学习网站[HDLBits](https://hdlbits.01xz.net/wiki/Main_Page), 它提供了丰富的习题, 并通过在线仿真判断你的代码是否正确.

如果你对自己的Verilog水平很有自信, 可以尝试完成以下习题:

- Sequential Logic  
  - Finite State Machines  
    - Lemmings 1 
    - Lemmings 2 
    - Lemmings 3 
    - Lemmings 4 
    - PS/2 packet parser 
    - PS/2 packet parser and datapath
    - Serial receiver
    - Serial receiver and datapath
    - Serial receiver with parity checking

如果你发现上述习题非常困难, 我们建议你从基础习题开始做起. 具体地, 你需要完成以下章节对应的习题:

- Getting Started - 建议完成, 以熟悉完成习题的流程
- Verilog Language - 建议不熟悉Verilog的同学完成
- Combinational Logic
  - Basic Gates
    - More logic gates
    - Truth tables
    - Simple circuit A
    - Simple circuit B
    - Thermostat
    - 3-bit population count
    - Even longer vectors
  - Multiplexers
    - 2-to-1 multiplexer
    - 9-to-1 multiplexer
    - 256-to-1 4-bits multiplexer
  - Arithmetic Circuits
    - Signed addition overflow
    - 100-bits binary adder
- Sequential Logic
  - Latches and Flip-Flops
    - D flip-flip
    - DFF with reset value
    - DFF with asynchronous reset
    - DFF with byte enable
    - Mux and DFF
    - Create circuit from truth table
    - Detect an edge
    - Detect both edges
    - Edge capture register
    - Dual-edge triggered flip-flop
  - Counters
    - Decade counter again
    - Slow decade counter
    - Counter 1000
    - 12-hour clock
  - Shift Registers
    - Left/right rotator
    - Left/right arithmetic shift by 1 or 8
    - 5-bits LFSR
    - Shift register
    - 3-input LUT
  - More Circuits - 全部完成
  - Finite State Machines - 全部完成

# 实例化

## 1.示例： 

~~~verilog
// 按位置连接
mod_a inst1 (a, b, out);

// 或按名称连接
mod_a inst1 (.in1(a), .in2(b), .out(out));
~~~

## 关于实例化有一个有意思的例子

![img](https://gitee.com/alps-chenyu/typora/raw/master/image/Module_shift.png)

~~~verilog
module top_module ( input clk, input d, output q );
    wire q2, q1;
    
    my_dff dff2(
        .clk(clk),
        .d(d),
        .q(q1)
    );
    
    my_dff dff1(
        .clk(clk),
        .d(q1),
        .q(q2)
    );
    
    my_dff dff0(
        .clk(clk),
        .d(q2),
        .q(q)
    );
endmodule

~~~

## 第二个例子

>这个例子虽然我写出来了， 但是我还是有不理解的地方

![img](https://gitee.com/alps-chenyu/typora/raw/master/image/Module_shift8.png)

~~~verilog
module top_module ( 
    input clk, 
    input [7:0] d, 
    input [1:0] sel, 
    output [7:0] q 
);
    wire [7:0] q1, q2,q3;
    
	// 先来描述移位寄存器
    my_dff8 inst0(
        .clk(clk),
        .d(d),
        .q(q1)
    );
    
    my_dff8 inst1(
        .clk(clk),
        .d(q1),
        .q(q2)
    );
    
    my_dff8 inst2(
        .clk(clk),
        .d(q2),
        .q(q3)
    );
    
    // 再来描述数据选择器
    
    always@(*) begin
        if(sel == 2'd0) begin 
        	q <= d;
      	end
        
        else if(sel == 2'd1) begin
        	q <= q1;
        end
        
        else if(sel == 2'd2) begin
           q <= q2; 
        end
        
        else if(sel == 2'd3) begin
        	q <= q3;
        end
    end
    
endmodule 

~~~

参考代码则是写的简介地多

~~~verilog
module top_module (
	input clk,
	input [7:0] d,
	input [1:0] sel,
	output reg [7:0] q
);

	wire [7:0] o1, o2, o3;		// output of each my_dff8
	
	// Instantiate three my_dff8s
	my_dff8 d1 ( clk, d, o1 );
	my_dff8 d2 ( clk, o1, o2 );
	my_dff8 d3 ( clk, o2, o3 );

	// This is one way to make a 4-to-1 multiplexer
	always @(*)		// Combinational always block
		case(sel)
			2'h0: q = d;
			2'h1: q = o1;
			2'h2: q = o2;
			2'h3: q = o3;
		endcase

endmodule

~~~

## 关于我多此一举还写错了这件事情

![img](https://gitee.com/alps-chenyu/typora/raw/master/image/Module_fadd.png)

~~~v
module top_module (
    input [31:0] a,
    input [31:0] b,
    output [31:0] sum
);
    // #1. 创建中间变量
    wire [15: 0] ha, hb, hsum, lsum;
    wire cout2;
    wire [15: 0] o;
    
    assign ha = a[31:16];
    assign hb = b[31:16];
    
    // #2. 用add1 构建 add16
	// #2.0  由于过于繁琐， 这里按照位置来进行实例化
    // #2.1 输入1 : la[15] - la[0]
    // #2.2 输入2 : lb[15] - lb[0]
    // #2.3 输出3 : lsum[15] - lsum[0]
    
    add1 add_0  (a[ 0], b[ 0],  1'b0, lsum[ 0], o[ 0]);
    add1 add_1  (a[ 1], b[ 1], o[ 0], lsum[ 1], o[ 1]);
    add1 add_2  (a[ 2], b[ 2], o[ 1], lsum[ 2], o[ 2]);
    add1 add_3  (a[ 3], b[ 3], o[ 2], lsum[ 3], o[ 3]);
    add1 add_4  (a[ 4], b[ 4], o[ 3], lsum[ 4], o[ 4]);
    add1 add_5  (a[ 5], b[ 5], o[ 4], lsum[ 5], o[ 5]);
    add1 add_6  (a[ 6], b[ 6], o[ 5], lsum[ 6], o[ 6]);
    add1 add_7  (a[ 7], b[ 7], o[ 6], lsum[ 7], o[ 7]);
    add1 add_8  (a[ 8], b[ 8], o[ 7], lsum[ 8], o[ 8]);
    add1 add_9  (a[ 9], b[ 9], o[ 8], lsum[ 9], o[ 9]);
    add1 add_10 (a[10], b[10], o[ 9], lsum[10], o[10]);
    add1 add_11 (a[11], b[11], o[10], lsum[11], o[11]);
    add1 add_12 (a[12], b[12], o[11], lsum[12], o[12]);
    add1 add_13 (a[13], b[13], o[12], lsum[13], o[13]);
    add1 add_14 (a[14], b[14], o[13], lsum[14], o[14]);
    add1 add_15 (a[15], b[15], o[14], lsum[15], o[15]);
    
    add16 add16_1 (ha, hb, o[15], hsum, cout2);
    
    // #3. 最后的连线 ： 
    assign sum = {hsum, lsum};
    
endmodule

module add1 ( input a, input b, input cin,   output sum, output cout );
  
    // 写一个全加器
    assign {cout, sum} = a + b + cin;
      
endmodule
~~~

>实际上是这题目的意思我理解错了， 实际上是题目已经帮我写好了add16 模块， 我们只需要做好add16的引脚引用就好了。

# 芯片设计的暴力美学

之前在F4阶段设，我计过加法器，大概是长这样：

 ![image-20260729153753014](https://gitee.com/alps-chenyu/typora/raw/master/image/image-20260729153753014.png)

上面设计的8位置全加器， 实际上就是只有前一位加好了，产生了进位， 才能继续往后加， 但实际上这是不好的。

因此产生了一种新的思想， 就是把所有运算的结果都提前先算好了，然后再使用数据选择器选择一个正确的结果。 

# 关于加法器和减法器

>实际上的加法器操作， 就是a + b + 0， 这都不用理解， 用直觉思考就好
>
>但是减法器操作 a - b, 还可以理解为 a + ~b + 1的操作， 这也是我之前没有使用的技巧，现在再HDL语言中可以用来写Verilog
